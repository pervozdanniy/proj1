syntax = "proto3";

package skopa.auth;

service AuthService {
  rpc Login (AuthRequest) returns (AuthData) {};
}

message AuthRequest {
  string login = 1;
  string password = 2;
}

message AuthData {
    string access_token = 1;
}