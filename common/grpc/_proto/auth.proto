syntax = "proto3";

package auth;

service AuthService {
  rpc Login (AuthRequest) returns (AuthData) {};
}

message AuthRequest {
    string login = 1;
    string password = 2;
}

message AuthData {
    string accessToken = 1;
    string refreshToken = 2;
}